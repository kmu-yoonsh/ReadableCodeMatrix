#include <iostream>

int main()
{
    int a, b, c;
    a = 1;
    b = 2;

    if(a < b)
        std::cin >> a >> b >> c;
    else
        std:cin >> a;

    if(a == b)
            std:cin >> b;

    return 0;
}